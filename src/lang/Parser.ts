export default abstract class Parser {
  /*
  parse: (token: string): Object => {
    let command: any = { code: '', args: [] };
    const match = token.match(/(.+)\((.*)\)/);
    if (!match) return command;
    command.code = match[1];
    command.args = match[2].split(',');
    return command;
  },
  */
}
